<!-- Header -->
<%= render 'header' %>

<!-- Flash Section -->
<% if !flash.empty? %>
    <%= render 'layouts/flash' %>
<% end %>

<!-- Stephanie Who? Section -->
<%= render 'stephaniewho' %>

<!-- A Giraffe Section -->
<%= render 'agiraffe' %>

<!-- Goethe Quote -->
<%= render 'quote1' %>

<!-- Work With Me Section -->
<%= render 'workwithme' %>

<!-- Process Section -->
<!-- Pending until we figure out if we want to use this section. Might need to rename. -->

<!-- Newsletter Signup Section -->
<%= render 'powerup' %>

<!-- Dr. Seuss Quote -->
<%= render 'quote2' %>

<!-- Portfolio Carousel Section-->
<!-- Pending until we get portfolio information. -->

<!-- Testomonials Section -->
<!-- Pending until we get testimonials. -->

<!-- Get In Touch Section -->
<%= render 'getintouch' %>

<!-- Blog Section -->
<!-- Pending until I implement blog feature. -->

<!-- Footer -->
<%= render 'footer' %>




