<section id="admin" class="services bg-coolGrey">
    <div class="container">
    <h1>Admin Panel</h1>
    <% if !flash.empty? %>
      <%= render 'shared/flash_no_section' %>
    <% end %>
        <%= link_to "Users", users_path, class: "btn btn-primary btn-lg btn-block" %>
        <%= link_to "Leads", leads_path, class: "btn btn-primary btn-lg btn-block" %>
    </div>
</section>